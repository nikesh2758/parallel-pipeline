pipeline {
  agent none
  stages {
    stage ('build') {
       steps {
       agent { label 'java' }
          sh '''
                  echo "this is build stage"
                  sleep 2
             '''
             }
    }
    stage ('deploy') {
 parallel {

    stage ('deploy1') {
       steps {
       agent master
          sh '''
                  echo "this is deploy stage"
                  sleep 2
             '''
             }
    }
    stage ('deploy2') {
       steps {
       agent master
          sh '''
                  echo "this is deploy stage"
                  sleep 2
             '''
             }
    }
    }
    }
    stage ('test') {
       steps {
       agent {label 'cprog' }
          sh '''
                  echo "this is deploy stage"
                  sleep 2
             '''
             }
    }
  }
}  
